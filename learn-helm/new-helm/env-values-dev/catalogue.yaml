component_name: "catalogue"

deployment:
    enabled: true

statefulset:
    enabled: false



replicas: 2


configMapValues:
    MONGO_URL: mongodb://mongo-dev.mikeydevops1.online:27017/catalogue
    MONGO: "true"
schema:
    image: crysis307/catalogue-db:latest
    enabled: true
    jobs:
        - master-data
    extention: js



service:
    type: ClusterIP
    port: 8080
    targetPort: 8080

resources:
    requests: { cpu: "50m", memory: "64Mi" }
    limits:   { cpu: "300m", memory: "256Mi" }

appPort: 8080
probeType: http

namespace: "roboshop"

hpa:
    enabled: true
    minReplicas: 2
    maxReplicas: 6
    targetCPUUtilizationPercentage: 70